~ 20231207 까지의 구현 {
	10개의 옷을 입력하고 종류를 구분하면 입력한 옷들의 종류가 구별되어 출력되었음.
	@ 문제점
	-> 1. 배열의 크기를 나타내는 값은 상수만 들어갈 수 있기에 오직 10개의 옷만을 입력할 수 있었음.
	-> 2. 현재 가진 옷들을 분류하여 출력할 때 함수를 사용했는데 잘못된 매개변수의 활용으로 함수 속 반복문이 배열의 값을 초과하는 공간의 출력을 강제하여
	     이상한 문자들이 출력되었음.
}

- 20231207 {
	오늘 구현한 내용 : 입력하고 싶은 옷의 개수를 입력한 뒤, 옷들을 입력하고 종류를 구분하면 입력한 옷들이 출력된다.
		..이중포인터의 동적 메모리 할당을 도입하여 앞선 문제점 1 해결.
		..매개변수를 수정하여 문제점 2 해결.
	
	@ 문제점
	-> 1. 다소 자주 사용되는 코드 : 함수화, 헤더파일화 필요 <- 일단 전체적인 틀 구현 후 코드 최적화 예정
	-> 2. 이중포인터(이차원배열)의 할당에서 버퍼 오버런 발생 <- 옷들의 이름은 전부 크기가 다르기에 string.h에서 strlen 활용 시 해결 가능해 보임.

	@ 앞으로 구현할 내용
	-> 1. 옷들의 색과 실루엣 입력 후 저장, 이에 따라서 앞으로의 옷 추천
	-> 2. 옷 추천을 위한 체형 분석 : 구조체를 통한 체형(키, 몸무게, 상체발달형, 하체발달형, 상체 비만, 하체 비만 등)의 입력
	-> 3. 체형에 따른 코디 추천(키, 몸무게를 통해 bmi 계산 함수 구현, 어울리는 색 공식을 입력하여 공식에 따라 발달형은 강조, 약점은 숨기는 방식으로)

	@ 한 줄 코멘트
	--- 이중포인터, 동적 메모리 할당, 구조체의 벽에 막혀 앞선 문제점 해결과 앞으로의 코딩에 어려움이 있었고, 현재는 이겨내는 중 입니다...
}

- 20231208-01 {
	구현한 내용 : (20231207) 앞으로 구현할 내용 1번
		..다수의 배열 동적 메모리 할당에서 복잡함을 느낌 -> 구조체를 통해 코드 간단화
		..옷 하나의 이름과 종류, 색, 핏을 입력하도록 설계 (전에는 옷들의 이름 전부 입력 후 ,종류 전부 입력이었음)

	@ 앞으로 구현할 내용
	-> 1. 옷 추천을 위한 체형 분석 : 구조체를 통한 체형(키, 몸무게, 상체발달형, 하체발달형, 상체 비만, 하체 비만 등)의 입력
	-> 2. 체형에 따른 코디 추천(키, 몸무게를 통해 bmi 계산 함수 구현, 어울리는 색 공식을 입력하여 공식에 따라 발달형은 강조, 약점은 숨기는 방식으로)
	
	@ 한 줄 코멘트
	--- 지금까지 썼던 코드식을 다시 바꾸는 것에 염증을 느껴 새로운 코드 식을 작성하였습니다.
		(옷의_조합과_추천.c file -> 프로젝트_ver2.c file 로 변경)
}






